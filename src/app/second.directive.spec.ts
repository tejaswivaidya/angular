import { SecondDirective } from './second.directive';

describe('SecondDirective', () => {
  it('should create an instance', () => {
    const directive = new SecondDirective();
    expect(directive).toBeTruthy();
  });
});
